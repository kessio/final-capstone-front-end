/* eslint-disable react/no-unescaped-entities */
import React from 'react'

function ReserveMotorcycle() {
  return (
    <div>
      <h2>ReserveMotorcycle page</h2>
      <p>
        To reserve an appointment, the user has to select a date and city
        (username and selected item are autofilled).Use the design based on the "Book a
        vespa test-ride" and add all necessary inputs. The user can also access the "Reserve"
        page from the navigation panel. In that case only username is autofilled.
      </p>
    </div>
  );
}

export default ReserveMotorcycle;
